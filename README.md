# 微说 (Weishuo) 社区

微说 (Weishuo) 是一个基于 Spring Boot 和原生 HTML/JS 开发的轻量级微博客平台。它聚合了全球实时热点新闻，并为用户提供了一个简洁、现代化的社交互动环境。

本项目作为课程设计作品，展示了前后端分离架构、第三方 API 集成以及基础的用户认证与内容管理功能。

---

## 📚 目录

- [项目概述](#-项目概述)
- [功能特性](#-功能特性)
- [技术栈](#-技术栈)
- [系统架构](#-系统架构)
- [快速开始](#-快速开始)
  - [环境要求](#环境要求)
  - [后端启动](#后端启动)
  - [前端启动](#前端启动)
- [API 接口说明](#-api-接口说明)
- [项目结构](#-项目结构)
- [未来规划](#-未来规划)

---

## 📖 项目概述

**微说** 旨在打造一个“小而美”的信息聚合社区。用户可以在这里：
1.  **看世界**：通过对接 Mediastack API，实时获取科技、体育、娱乐等多个领域的全球资讯。
2.  **聊热点**：查看实时热搜榜单，了解当前最受关注的话题。
3.  **轻社交**：注册账号，拥有自己的个人主页，对感兴趣的内容进行点赞和互动。

---

## ✨ 功能特性

### 🌟 技术亮点
*   **云端数据库集成**：采用 Google Cloud SQL (MySQL 8) 作为生产环境数据库，实现数据的云端持久化存储，保障数据安全性和高可用性。

### 1. 用户系统
*   **注册与登录**：支持邮箱/用户名注册，密码加密存储，保障账户安全。
*   **个人资料**：用户拥有独立的个人中心，展示头像、昵称及简介。

### 2. 新闻资讯
*   **多频道聚合**：内置热门、科技、视频、社会、体育、时尚等 10+ 个新闻频道。
*   **实时更新**：后端直连第三方新闻源，确保资讯的时效性。
*   **智能降级**：当外部 API 不可用时，系统自动切换至本地兜底数据，保证页面不留白。

### 3. 交互体验
*   **热搜榜单**：右侧悬浮热搜榜，实时展示高热度话题。
*   **滚动热词**：顶部跑马灯设计，直观展示搜索趋势。
*   **详情浏览**：点击卡片平滑跳转至详情页，支持查看完整新闻内容。
*   **响应式设计**：完美适配桌面端与移动端浏览器。

---

## 🛠 技术栈

### 后端 (Backend)
*   **语言**：Java 17
*   **框架**：Spring Boot 3.3.2
*   **数据库**：Google Cloud SQL (MySQL 8) - 云端持久化存储 ☁️
*   **ORM**：Spring Data JPA
*   **工具**：Lombok, Maven

### 前端 (Frontend)
*   **核心**：HTML5, CSS3, JavaScript (ES6+)
*   **通信**：Fetch API
*   **样式**：原生 CSS (Flexbox/Grid 布局)，无繁重 UI 框架依赖

### 外部服务
*   **数据源**：[Mediastack API](https://mediastack.com/) (全球免费新闻 API)

---

## 🏗 系统架构

```mermaid
graph TD
    User[用户浏览器] -->|HTTP/JSON| Frontend[前端 (HTML/JS)]
    Frontend -->|Fetch API| Backend[后端 (Spring Boot)]
    Backend -->|JPA| CloudDB[☁️ Google Cloud SQL]
    Backend -->|REST| ExternalAPI[Mediastack API]
    CloudDB -->|数据持久化| Storage[(MySQL 8)]
```

---

## 🚀 快速开始

### 环境要求
*   **JDK 17+**
*   **Maven 3.6+** (项目内置 mvnw)
*   **Python 3** (可选，用于快速启动前端服务) 或 VS Code Live Server

### 后端启动
1.  进入后端目录：
    ```bash
    cd backend
    ```
2.  运行 Spring Boot 应用：
    *   Mac/Linux: `./mvnw spring-boot:run`
    *   Windows: `mvnw spring-boot:run`
3.  后端服务将在 `http://localhost:8080` 启动。

### 前端启动
1.  进入前端目录：
    ```bash
    cd frontend
    ```
2.  启动静态服务器 (推荐使用 Python 内置服务)：
    ```bash
    python3 -m http.server 5501
    ```
3.  在浏览器访问：[http://localhost:5501/login.html](http://localhost:5501/login.html)

---

## 🔌 API 接口说明

| 模块 | 方法 | 路径 | 描述 |
| :--- | :--- | :--- | :--- |
| **认证** | POST | `/api/auth/register` | 用户注册 (参数: username, password, email) |
| **认证** | POST | `/api/auth/login` | 用户登录 (参数: username, password) |
| **新闻** | GET | `/api/news/latest` | 获取最新新闻 (参数: channel) |

---

## 📂 项目结构

```text
Weishuo-Project
├── backend/                  # 后端工程
│   ├── src/main/java/        # Java 源代码
│   │   └── com/weishuo/backend
│   │       ├── auth/         # 认证模块 (Controller, Service)
│   │       ├── news/         # 新闻模块 (对接外部 API)
│   │       ├── user/         # 用户模块 (实体定义)
│   │       └── config/       # 全局配置 (CORS, Properties)
│   └── src/main/resources/   # 配置文件 (application.yml)
│
└── frontend/                 # 前端工程
    ├── scripts/              # JavaScript 逻辑
    │   ├── app.js            # 核心业务 (新闻流、热搜)
    │   ├── auth.js           # 认证逻辑 (登录注册)
    │   └── story.js          # 详情页逻辑
    ├── styles/               # CSS 样式表
    └── *.html                # 页面文件
```

---

## 📅 未来规划

1.  **安全增强**：引入 Spring Security 和 JWT Token 机制，实现更安全的无状态认证。
2.  **评论功能**：开发完整的评论系统，支持用户对新闻发表观点。
3.  **内容发布**：完善发布器功能，允许用户撰写并发布原创图文微博。
4.  **数据分析**：集成 Google Cloud Analytics，实现用户行为分析与可视化。

---

**Author**: CCL
**Date**: 2025-12-03
